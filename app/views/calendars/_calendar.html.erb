<li class="<%= calendar.parent? ? 'list-group-item' : 'sub-list' %>">
  <label class="divBox">
    <%= check_box_tag "calendar-select-id-#{calendar.id}",
      calendar.id, true, class: "calendar-select" %>
    <label id="<%= "label-calendar-select-#{calendar.id}" %>"
      class="<%= "color-#{calendar.get_color(current_user.id)}" %>"
      for="<%= "calendar-select-id-#{calendar.id}" %>">
    </label>
    <%= calendar.name %>
  </label>
  <span id="<%= calendar.id %>"
    class="clstMenu clstMenu-child" rel="<%= calendar.get_color current_user.id %>">
    <% unless calendar.parent? %>
      <span class="sub"></span>
    <% end %>
  </span>
</li>
