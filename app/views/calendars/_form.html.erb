<%= form_for @calendar do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name, required: true %>
  </div>
  <div>
    <%= f.label :description %>
    <%= f.text_area :description, required: true %>
  </div>
  <div>
    <%= f.submit t "calendar.save" %>
  </div>
<% end %>
