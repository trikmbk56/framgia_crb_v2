<th><%= t ".repeat_on" %></th>
<td>
  <div>
    <span class="ep-rec-dow">
      <%= f.fields_for :repeat_ons do |repeat_builder| %>
        <%= repeat_builder.hidden_field :repeat_on %>
        <%= repeat_builder.check_box :_destroy,
          {
            checked: repeat_builder.object.persisted?,
            id: "repeat-#{repeat_builder.index}"
          },
          false, true %>
        <%= repeat_builder.label :_destroy, repeat_builder.object.repeat_on %>
      <% end %>
    </span>
  </div>
</td>
